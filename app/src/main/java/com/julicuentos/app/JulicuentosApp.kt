package com.julicuentos.app

import android.app.Application

/**
 * Application stub for the slice 1 scaffold.
 * Slice 2 will keep this class minimal: the process-scoped PlaybackRepository
 * is created lazily, so no wiring belongs here yet.
 */
class JulicuentosApp : Application()
