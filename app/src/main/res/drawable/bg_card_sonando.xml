<?xml version="1.0" encoding="utf-8"?>
<!-- "Currently playing" card frame (design-consult-opus.md C11): surface card with
     a 4 dp mint edge — the whole card announces the active story. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/superficie" />
    <stroke android:width="4dp" android:color="@color/accion" />
    <corners android:radius="@dimen/corner_card" />
</shape>