<?xml version="1.0" encoding="utf-8"?>
<!-- Miniplayer bar: surface #28244B, top corners 12 dp — sits flush to the bottom of the
     screen (specs/catalog "Miniplayer"). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/superficie" />
    <corners
        android:topLeftRadius="@dimen/corner_mini"
        android:topRightRadius="@dimen/corner_mini"
        android:bottomLeftRadius="0dp"
        android:bottomRightRadius="0dp" />
</shape>