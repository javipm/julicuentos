<?xml version="1.0" encoding="utf-8"?>
<!-- Miniplayer card (design-consult-opus.md C8): floating card — surface, 20 dp
     corners on ALL four sides, 1 dp hairline border; the 8 dp screen margins make
     it read as a card of the same family as the story tiles, not chrome. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/superficie" />
    <stroke android:width="1dp" android:color="@color/borde" />
    <corners android:radius="@dimen/corner_mini" />
</shape>