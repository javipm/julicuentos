<?xml version="1.0" encoding="utf-8"?>
<!-- Miniplayer play/pause chip: mint rounded square, corner ratio 0.22 of the
     52 dp side ≈ 12 dp (theme-design "Minimum touch targets", specs/catalog miniplayer). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/accion" />
    <corners android:radius="@dimen/corner_play_chip" />
</shape>