<?xml version="1.0" encoding="utf-8"?>
<!-- Shared surface row (queue rows, unselected timer options): #28244B on the
     #17152E background, 16 dp corners — depth by color, no elevation (S5.1/S5.2). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/superficie" />
    <corners android:radius="@dimen/corner_card" />
</shape>
