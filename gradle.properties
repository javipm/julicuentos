# Gradle daemon JVM (host JDK 17). Larger heap for AAPT2 linking the ~650 MB asset APK.
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8

# AndroidX is mandatory for all Android Support Library artifacts (design.md D9).
android.useAndroidX=true
