# Verify Report — migrate-to-native-android

- **Schema:** spec-driven · **Artifact store:** openspec · **Change root:** `openspec/changes/migrate-to-native-android`
- **Verify date:** 2026-08-31 (host session) · **Verifier:** SDD VERIFY executor (read-only; report only)
- **Scope:** HOST-VERIFIABLE only. Device items are a recorded owner decision (`deferred-device`) and are marked DEFERRED-DEVICE with the DV id; they are not failures.

- **Commit range reviewed:** `2e084df` (baseline) → `693eebf` (slice 5). Working tree clean at verify time; APK rebuilt fresh during this verify (forced `--rerun-tasks` on both build and tests).
- **Assets:** `app/src/main/assets/` untouched by this verify. The catalog is owner-managed and now carries **64 stories** (verified: `stories.json` count 64, ids unique + alphabetical, every `cover`/`thumbnail` ref resolves to a file, no `fechaPublicacion`). Requirements worded "exactly 20 stories" are evaluated as "the owner- managed catalog carried in `stories.json` (64 at verify time)", per the owner decision recorded in the change status and the review-ledger process note.

## Summary verdict: **PASS-WITH-DEFERRED**

| Verdict bucket | Count |
|---|---|
| PASS (host-verifiable, incl. PASS with device-note) | 49 |
| PARTIAL (1: delivery manifest — library-inherited `ACCESS_NETWORK_STATE`) | 1 |
| FAIL | **0** |
| DEFERRED-DEVICE (requirements that explicitly need the Fire HD 10) | 3 (Playback P8/P9, Delivery D5) |
| **Total requirements** | **53** |

No host-verifiable FAILs found (no code-contradicts-spec condition). One PARTIAL is reported (delivery "Manifest and permissions"): the merged manifest carries `android.permission.ACCESS_NETWORK_STATE` inherited from the pinned `androidx.media3:media3-common`/`media3-exoplayer` 1.2.1 AARs. `INTERNET` is ABSENT (cannot make a network request even by accident), the app's own manifest declares exactly the 4 spec'd permissions, and `ACCESS_NETWORK_STATE` (normal protection) grants no network I/O — but the spec's literal wording ("exactly 4 … or any other network permission") is not met at merge level. Owner decision required (see Risks R-1). All device acceptance items are tracked as DV1/DV3–DV11.



# 1. Build + test evidence (fresh runs during verify)

| Check | Command | Exact result |
|---|---|---|---|
| Assemble (forced) | `./gradlew assembleDebug --rerun-tasks` | `BUILD SUCCESSFUL in 19s — 35 actionable tasks: 35 executed` |
| Unit tests (forced) | `./gradlew testDebugUnitTest --rerun-tasks` | `BUILD SUCCESSFUL in 4s — 23 actionable tasks: 23 executed` |
| Test count | `app/build/test-results/testDebugUnitTest/*.xml` | **61 tests, 0 failures, 0 errors** — TimeFormatTest 5, PersistedStateParserTest 18, RestoreCoordinatorTest 7, QueueStoreTest 19, TimerLogicTest 12 |
| APK | `app/build/outputs/apk/debug/app-debug.apk` | 1,254,097,154 bytes; badging: `package: name='com.julicuentos.app' versionCode='5' versionName='1.0'`, `sdkVersion:'22'`, `targetSdkVersion:'34'`, compileSdk 34, label "Julicuentos" |
| Permissions | `aapt dump permissions` (build-tools 34.0.0) | App-declared (exactly the spec set): FOREGROUND_SERVICE, FOREGROUND_SERVICE_MEDIA_PLAYBACK, WAKE_LOCK, POST_NOTIFICATIONS. Library-inherited: `android.permission.ACCESS_NETWORK_STATE` (media3-common/exoplayer 1.2.1 AARs) + `com.julicuentos.app.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION` (androidx.core; documented precedent). **`INTERNET` ABSENT**. |
| MP3 packaging | `unzip -v app-debug.apk` | **64** `assets/audio/*.mp3` entries, all **Stored** (0 % compression, noCompress working). None Deflate. |

Notes: build outputs regenerated by forcing `--rerun-tasks` (no incremental masking); network-free (wrapper uses the local Gradle dist `tools/gradle-dist/gradle-8.7-bin.zip`; no dependencies fetched). Pre-existing compile warnings only: `adapterPosition` deprecation (`StoryAdapter.kt`), and `Unreachable code` at `AudioSourceResolver.kt:110` — a benign Kotlin warning inside the `synchronized` non-local-return block; no behavioral effect (the copy-fallback still returns target/null correctly). Test JUnit counts match the review-ledger's "61 unit tests" claim.



# 2. Spec traceability sweep (53 requirements)

Legend: **PASS** = host-verifiable code evidence found (file:line given;; **PARTIAL** = host evidence with a documented gap;; **FAIL** = code contradicts spec (none);; **DEFERRED-DEVICE** = requires the Fire HD 10 (DV id in parentheses; recorded owner decision already exists in tasks.md/docs.

### specs/catalog (7)

| Req | Verdict | Evidence |
|---|---|---|---|
| C1 Compiled-in catalog data | PASS | `assets/stories.json` (64 entries, unique + alphabetical, all refs resolve; no `fechaPublicacion`); `catalog/CatalogParser.kt` (validator-first, drops bad/missing id or titulo, coerce duracion ≥0); `catalog/StoryRepository.kt` (lazy singleton, empty-on-corrupt, no crash); content fixes present in the JSON and asserted by `tools/port-catalog.py` (bambi starts "Bambi es…", title "Los Increíbles", R1-002). No network load path anywhere. |
| C2 Adaptive grid columns | PASS (DV9) | `res/values/integers.xml`=2 / `values-w768dp`=3 / `values-w1024dp`=5 → `GridLayoutManager(span)` (`CatalogFragment.kt:35`); grid padding 12 dp h / 32 dp bottom (16 with miniplayer, `grid_padding_bottom_mini`). Fire HD 10 column counts + rotation → DV9. |
| C3 Story card composition | PASS | `res/layout/item_story_card.xml`: 1:1 square cover, Fredoka 16 2-line title, Nunito 14 2-line synopsis `#AAA3CE`, duration chip (`TimeFormat.formatTime`, `StoryAdapter.kt`), 52 dp ⋮ (`min_touch`) with `scrim_overflow` (#8C17152E), surface `#28244B` 16 dp corners (`bg_card.xml`), no elevation; "Sonando" pill (`#72E0B8` bg, `#17152E` bold text, Badge style) overlaid on the image, moved via `notifyItemChanged(payload)` only (`StoryAdapter.setCurrentStory`). |
| C4 Story action sheet (⋮) | PASS | `ui/catalog/StoryActionSheet.kt` + `res/layout/dialog_story_actions.xml`: rows `minHeight=@dimen/action_row_min` (52 dp), 16 dp corners; scrim via dialog theme (`backgroundDimEnabled`); "Reproducir ahora" (solid mint,→ `repo.load(id,0,true)` + open), "Añadir a la cola" (mint outline,→ `repo.enqueue(id)`),"Cancelar" (`dismiss()`). |
| C5 Row tap semantics (resolveOpenStoryAction) | PASS | `CatalogFragment.kt:87-98`:current+Playing → open only (no reload); current+Paused → toggle+open;else → `repo.load(id,0,autoplay=true)` + open immediately (no await of prepare). **Play-now preserves the queue** (load never touches `QueueStore`; Q4). |
| C6 Miniplayer | PASS | `res/layout/view_mini_player.xml`: 64 dp bar + 6 dp strip + 8 dp bottom inset, 48 dp cover, 52 dp mint chip (`bg_play_chip`, corner ratio 0.22), 52 dp next → `repo.playNext()`; `MiniPlayerView.kt` (bind after inflate, R3-001), `MiniProgressStrip.kt` `isEnabled=false` (non-interactive). In-flow footer in `fragment_catalog.xml` (never an overlay), GONE when `currentStory == null` (`CatalogFragment.updateMiniVisibility`). |
| C7 Grid thumbnail budget (no OOM) | PASS (DV9) | `media/ThumbCache.kt`: `LruCache(minOf(12MB, heap/8))`, decode 512 px RGB_565 via inSampleSize, single-thread executor + main-post; `media/Bitmaps.kt` 2-entry ≤640 px cover cache; grid uses thumbs only. 5-column landscape fling on Fire → DV9. |

### specs/playback (12)

| Req | Verdict | Evidence |
|---|---|---|---|
| P1 Single ExoPlayer source of truth | PASS | `playback/PlaybackService.kt` (the ONLY `ExoPlayer` in the single `MediaSessionService`); `playback/PlaybackRepository.kt` (process-scoped singleton, one `MediaController`); `playback/PlayerState.kt` (sealed Idle/Loading/Ready/Playing/Paused/Ended/Error, one derived state). `setMediaItem` releases the previous source before prepare (P2). |
| P2 Load-story contract | PASS | `PlaybackRepository.performLoad`: the Loading state paints FIRST, async `resolveAsync` with `loadGen` generation guard (R2-004 fixes the blocking-worker ANR), `setMediaItem → prepare → play()` only when autoplay. Stale events discarded via `isCurrentItem` gate (`onPlaybackEvent`, `onPlayerErrorInternal`, R2-003). |
| P3 Play/pause and transport | PASS | `togglePlayPause`, `skipBy(±15_000)` → `seekTo` clamped `coerceIn(0, duration)` with catalog-duration fallback until metadata (R2-006). No previous-track/shuffle/repeat/speed/chapters anywhere (grep + layouts). |
| P4 Seek = drag-preview + commit-on-release | PASS (DV5) | `ui/player/SeekBarController.kt`: drag start → `dragging=true` (progress writes suppressed); during drag → thumb tracks the finger + preview `formatTime(ratio*duration)`, ZERO player calls; release → exactly ONE `onCommit → repo.seekTo(target)`; `pendingTargetMs` hold prevents thumb snap-back after commit (R4-001); `seekBar.max` updated regardless of dragging (R4-002). Both player layouts have **NO ScrollView** (grep → comment only), so scrub can never be stolen by a scroll gesture. Tap-to-seek = single-commit path. On-device thumb/no-snap/one-seek → DV5. |
| P5 Autoplay rules | PASS | Every user start path passes `autoplay=true` (catalog tap, sheet play-now, miniplayer ⏭ `playNext`, queue auto-advance `onEnded`, retry `PlayerFragment.retry`); cold restore is the ONLY `autoplay=false` (`hydrate → performLoad(…, false)`). |
| P6 Duration from real metadata | PASS | `PlayerFragment.onProgressInternal`: first snapshot with `durationMs > 0` replaces catalog placeholder (`seekController.setDuration(real)`); seek range + end label then follow real duration (S4.7). |
| P7 End of story vs manual next (queue asymmetry) | PASS | `PlaybackRepository.onEnded()`:queue head consumed + autoplay; **empty queue → `pause()` + Ended, NO catalog wrap, queue preserved**; EndOfStory timer → pause + no advance. `playNext()`: `takeNext(...) ?: circularNext(...)` — **manual ⏭ wraps alphabetically** (`circularNext` = index+1 mod size). |
| P8 Background playback (success criterion a) | **DEFERRED-DEVICE (DV4)** | Host config present + correct: FGS `foregroundServiceType="mediaPlayback"` in manifest, `setWakeMode(WAKE_MODE_LOCAL)` (partial wake lock while playing), `onTaskRemoved` stop-if-not-playing. The ≥30 min screen-off run on the real Fire HD 10 is DV4. |
| P9 Media notification and media buttons | **DEFERRED-DEVICE (DV3)** | Host: custom `MediaNotificationProvider` (NOT Default), `NotificationCompat.MediaStyle` from androidx.media (the spec fallback), channel only SDK≥26, 4 actions play/pause,−15,+15,next (compact 0–2), artwork ≤256 px RGB_565, Spanish labels (`notif_*`), `Bundle()` everywhere (R2-001 fixed — grep: no `Bundle.EMPTY` remains). Interactive behavior on the shade/lock screen on Fire OS 5.3 → DV3. |
| P10 Audio focus | PASS (device observation deferred) | `setAudioAttributes(USAGE_MEDIA, CONTENT_TYPE_MUSIC, handleAudioFocus=true)` — ExoPlayer AudioFocusManager implements the loss/transient/duck matrix on the API-22 legacy path; `setHandleAudioBecomingNoisy(true)` (unplug pauses, no speaker blast. Device-observable duck/loss rides acceptance/DV10. |
| P11 Error handling (bundled audio only) | PASS | Missing/unreadable audio → `surfaceError` → `PlayerState.Error`; UI maps to Spanish `error_audio_faltante`/`error_generico` (R4-004; diagnostic to logcat); Reintentar = `load(same id, attempted position,true)`; Cerrar = popBackStack. No streaming-recovery/retry-counters/network copy. |
| P12 Progress updates targeted, not global | PASS | 500 ms `progressRunnable` → `ProgressListener` only; catalog grid rebinds on state change only (payload `PAYLOAD_CURRENT`, `StoryAdapter.setCurrentStory`), queue submits on queue-mutation events only (`QueueFragment.onQueueChanged`, `QueueAdapter.submit`); position-driven widgets = seek bar, labels, halo ring, miniplayer strip. |

### specs/queue (6)

| Req | Verdict | Evidence |
|---|---|---|---|
| Q1 Ids-only queue model | PASS | `playback/QueueStore.kt` — ids only, catalog resolution by id in adapters; unknown ids dropped at restore (`persist/RestoreCoordinator.kt` filters against knownIds) — self-heals, no crash. `QueueStoreTest` 19 tests. |
| Q2 Enqueue append + de-duplicated | PASS | `InMemoryQueueStore.enqueue` appends unless already present (no dup, no reorder, no move-to-head/tail); "＋" = `repo.enqueue(current.id)`; membership for the disabled/✓ state via `isQueued`. |
| Q3 Reorder and remove | PASS | `moveUp`/`moveDown` swap adjacent + no-op at boundaries (no wraparound); `remove` filters by id; `clear` empties. QueueAdapter 48 dp visuals inside 52 dp targets (`item_queue_row.xml`), remove tinted peach (`ic_close_peach`). |
| Q4 Play-now preserves queue — **BEHAVIOR CHANGE** | PASS | `load()`/`performLoad()` never touch the queue (grep + code read: no clear/remove in the load path; `StoryActionSheet` play-now and `CatalogFragment` tap both route through `repo.load` only). RN's `playNowClearsQueue()` is not ported. |
| Q5 Next resolution and end-of-queue | PASS | `takeNext` consumes the head (and removes a current-in-queue entry so it can never re-arm itself); `peekNext` preview; manual ⏭ wraps via `circularNext`; natural end stops on empty queue (`onEnded`). Asymmetry paired with P7. |
| Q6 Queue screen | PASS | `fragment_queue.xml`: ✕ back, "Cola" (Fredoka 20), "Vaciar" peach/enabled ↔ soft/disabled (`QueueFragment.onQueueChanged`; rows min 72 dp, 56 dp cover, bold 2-line title, up/down/✕; empty-state copy = spec-verbatim `queue_empty` ("…Añade cuentos desde el menú ⋮ de una tarjeta o con el botón + del reproductor."). |

### specs/sleep-timer (6)

| Req | Verdict | Evidence |
|---|---|---|---|
| S1 Timer modes | PASS | `playback/TimerState.kt` sealed Off/Minutes/EndOfStory; `TimerLogic.isValidMinutes` = {15,30,45} else normalize-to-off; expired/invalid restores → Off (12 logic tests). |
| S2 Timer selection UI | PASS | `fragment_timer.xml`: helper copy (`temporizador_helper`, verbatim "+ 5 static 56 dp rows +":15/30/45,"Al terminar este cuento","Desactivar"; selected = `bg_timer_row_selected` (solid peach #FFB66E + dark #17152E bold), unselected = `bg_row_surface` (#28244B + #F8F7FF`; `TimerFragment.applyKey` applies immediately and pops back (`setTimerMinutes`/`setTimerEndOfStory`/`clearTimer`). |
| S3 Minutes countdown (screen-off-proof) | PASS (DV7) | `endsAt = SystemClock.elapsedRealtime() + minutes*60_000` (`TimerLogic.endsAtFor`), 1 Hz main-Handler tick anchored to elapsedRealtime, `remainingMs(now=elapsedRealtime())` — monotonic, never wall-clock; `endsAt` persisted (D3 anchor). Screen-off drift ≤5 s on Fire → DV7. |
| S4 Expiry = ~10 s fade, then pause, queue preserved — **BEHAVIOR CHANGE** | PASS (DV7) | Repository `tickTimer → expireTimer` (timer→Off, `flushNow()`, fade armed only if actually playing) → custom command `ACTION_TIMER_EXPIRE` → `PlaybackService.fadeRunnable`: 10 steps × 1 s `fadeVolumeAtStep` 0.9→0.0 (verified) then `pause()` + volume 1.0;per-step `isPlaying` re-check aborts → `finishFade()` (cancel rules: user pause/play/load/timer-change → `ACTION_TIMER_CANCEL_FADE`). Queue untouched on expiry. Fade acoustic observation on Fire → DV7. |
| S5 End-of-story timer suppresses auto-advance | PASS | `onEnded()` EndOfStory branch: timer→Off, `notifyTimer`, `pause()`, NO `takeNext`/wrap; queue preserved (markDirty). RN's dead `shouldPauseAtEndOfStory` not ported (minutes expiry stays fade-driven). |
| S6 Timer visibility on player | PASS | `PlayerFragment.bindTimer`: Minutes → `timer_line_minutes` (`Temporizador: %1$s`, `formatRemaining` ceil, 1 Hz), EndOfStory → `timer_line_end`, Off → GONE; `CoverHaloView.setTimerActive` mirrors onto the 3 dp peach ring; all cleared on expiry/off. |

### specs/persistence (7)

| Req | Verdict | Evidence |
|---|---|---|---|
| R1 Single storage key, fixed JSON schema | PASS | `persist/PlayerStore.kt`: exactly ONE key `julicuentos.player.v1`, one JSON string, one prefs file (grep: no other SharedPreferences usage; schema exactly `currentStoryId/positionMs/queueIds/timer{kind,minutes,endsAt}/updatedAt` (`PersistedState.toJson` via `MiniJson`); `updatedAt` written never read; single global resume position. |
| R2 Flush cadence (5 s + onStop) | PASS | `FLUSH_CADENCE_MS = 5000` dirty-tracked (`markDirty`/`flushRunnable`); `MainActivity.onStop → repository.flushNow()` (R5-001 fix; not on isFinishing); service `onDestroy → flushNow()`; expiry → immediate `flushNow()`; `commit()` atomic whole-string write. |
| R3 Never write before hydration | PASS | `hydrationComplete` gate in `markDirty()`/`flushNow()` — early return pre-hydration; `writeSnapshot` skips empty default state (no bogus blob on fresh install). |
| R4 Never overwrite user actions during restore | PASS | `userMutated` latch set by any command pre-hydration; `hydrate()` discards the snapshot wholesale (`RestoreCoordinator.applySnapshot=false`) and re-arms `markDirty()` (R5-002 fix: the user's fresh state persists via next tick/onStop); deferred user loads replay before hydrate (`onControllerReady`). |
| R5 Cold restore paused at last position (success criterion c) | PASS (DV6) | `hydrate()` → `RestoreCoordinator.decide(...)` → `performLoad(story, positionMs, autoplay=false)` + `restoreTimer` + `queue.setInitial` — paused by definition, never autoplay. Force-stop reopen on Fire → DV6. |
| R6 Tolerant parser with safe defaults | PASS | `PersistedStateParser` (18 unit tests): bad/truncated JSON → empty defaults; non-numeric/NaN/negative `positionMs` → 0;non-string `queueIds` entries filtered;`timer.minutes ∉ {15,30,45}`/missing/expired `endsAt` → Off (`TimerLogic.normalizeRestore`: reboot-artifact window + 24 h sanity cap — "no self-pause surprise"). |
| R7 Restore drops unknown ids | PASS | `RestoreCoordinator`: unknown `currentStoryId` → null (Idle, no miniplayer, no load); unknown queue ids filtered out; known ids restored unchanged, in order. |

### specs/theme-design (10)

| Req | Verdict | Evidence |
|---|---|---|---|
| T1 Locked palette tokens | PASS | `res/values/colors.xml`: 6 tokens (fondo #17152E, superficie #28244B, accion #72E0B8, temporizador #FFB66E, texto #F8F7FF, textoSuave #AAA3CE) + `track` #3D3860 + 4 ported literals (scrim_overflow #8C17152E, scrim_modal #80000000, ring_base #4772E0B8, track_seek #66AAA3CE) — all hex exact. Full hex audit of res/: only these + alfa variants + white icon-glyph fills (#FFFFFFFF on dark surfaces) + transparent dialog bg; no off-palette colors. |
| T2 Flat by hardware constraint | PASS (DV10) | `grep -rniE "elevation|shadow|blur" res/ java/` → comments/KDoc only ("no elevation"…); no `animated-vector`/`*anim*` resources; CoverHalo uses clipped round-rect (ViewOutlineProvider, no elevation)+ `LAYER_TYPE_SOFTWARE` (no per-frame alpha blending surprises); covers set 0 ms (no fade). Device flat audit → DV10. |
| T3 Minimum touch targets 52 dp | PASS (DV10) | `min_touch=52dp` (`values/dimens.xml:5) used by ⋮, sheet rows (52), transport/back/timer/queue buttons, miniplayer chip + next, "Ver cola"/error/empty (minWidth/minHeight 52);play button 72 dp (64 compact); queue buttons 48 dp visuals inside 52 dp targets; timer rows 56 dp. Device measure → DV10. |
| T4 Bundled fonts and TextAppearances | PASS (DV10) | 3 TTFs (`fredoka_semibold`, `nunitosans_regular`, `nunitosans_bold`) + `res/font/fredoka.xml`/`nunitosans.xml` (weights 600/400/700, style/weight wiring); `TextAppearance.Jc.{30,24,20,18,16,14,12}` ladder + Button (bold) + Badge;Fredoka SemiBold on display heads ("Julicuentos", titles, "Cola", "Temporizador", sheet), Nunito Sans 400/700 on body/button/badge. No text view declares the system default (no default fontFamily rendering — static audit) Device visual font check → DV10. |
| T5 Dual-orientation player layouts | PASS (DV10) | `layout/fragment_player.xml` portrait stack (non-scrolling, descr @integer lines 2, seek outside a scroll container)· `layout-w600dp-land/fragment_player.xml` split (cover left, 520 dp controls column, gap 28, maxWidth 1100, no scrolling) — engages on w≥600dp-land (Fire landscape 1280×752 → yes); `values-land` compact (gap 12, play 64, bottom 16, desc 1 line) vs `values-h720dp-land` roomy (24/72/32/2; Fire h752 picks roomy. Cover 340 dp = the clamp cap [120,340] and equals 0.42 × width at Fire landscape (1280), leaving ≥320 dp for controls (520 used]. |
| T6 Cover art square-ring progress | PASS (DV10) | `CoverHaloView.onDraw`: rounded square corner ratio 0.12 (clip, no elevation);base mint ring at alfa 0.28 (`ring_base`) constant; 4 square-ring segments light at thresholds **>2 %, >28 %, >55 %, >80 %** (exact, code); when timer active, a 3 dp peach #FFB66E outer ring (`setTimerActive`); one onDraw pass; no SVG/animation lib;cover bitmap never re-drawn on ticks (0 ms fade).. |
| T7 Time formatting | PASS | `common/TimeFormat.kt`: `formatTime(ms)` floors to whole seconds (m:ss, or h:mm:ss when hours>0; negatives → 0:00); `formatRemaining(ms)` ceils (always m:ss, "0:01" at 400 ms, reaches 0:00 only at expiry); overflow-safe ceil (R4-003);5 boundary tests green. |
| T8 Spanish strings registry | PASS | `res/values/strings.xml` complete verbatim registry ("Julicuentos", "Elige un cuento y empieza a escuchar", "REPRODUCIENDO", "Reproducir ahora", "Añadir a la cola", "Cancelar", "Sonando", "En pausa", "Ver cola", "Vaciar", "Desactivar", "Reintentar", "Cerrar", "Temporizador", "Cola", "Cargando audio…", "No hay ningún cuento en reproducción.", "Volver al catálogo", "Al vencer, se pausa la reproducción y se conserva la cola.",15/30/45,"Al terminar este cuento",+ corrected empty queue copy). Layouts reference only `@string` (grep: no hardcoded `android:text`), Kotlin UI has only the transient time default "0:00" (`SeekBarController.setDuration` — equals `formatTime(0)`, overwritten on first metadata tick;not copy). English repo/library diagnostics ARE mapped to Spanish in the UI (R4-004). `queue_added_ok` unused (harmless, noted. |
| T9 Launch, splash, system chrome | PASS (DV10) | Theme `windowBackground=@color/fondo` (dark splash, no white flash; holds until first frame), `statusBarColor=fondo`, `windowLightStatusBar=false` (ignored on API 22); launcher mipmaps mdpi48/hdpi72/xhdpi96/xxhdpi144 composited over #17152E (no adaptive on 22). Cold-start visual → DV10/acceptance. |
| T10 Performance budget (API 22) | PASS (DV9/DV10) | Flat solid fills only (no elevation/blur/animated vectors, grep-clean); position-driven widgets at ~2 Hz (500 ms) targeted cadence; recycled stable-id grid cells (`getItemId`, payload bind-once); shallow hierarchies; seek bar outside scroll containers; MiniProgressStrip/CoverHalo redraw only their own paths. Fling overdraw on device → DV9/DV10. |

### specs/delivery (5)

| Req | Verdict | Evidence |
|---|---|---|---|
| D1 Project scaffold and build | PASS | Gradle 8.7 wrapper pinned to the local dist (`gradle/wrapper/gradle-wrapper.properties` → file://…/tools/gradle-dist/gradle-8.7-bin.zip); AGP 8.5.2 / Kotlin 1.9.24 / media3 1.2.1 via `gradle/libs.versions.toml` (D9 pins);single `:app` module, `com.julicuentos.app`, minSdk 22 / compileSdk 34 / targetSdk 34, Kotlin + XML views (no Compose), no desugaring; `assembleDebug` green (forced rerun this verify); versionCode 5 monotonic across slices (1→2→3→4→5, verified in git history + badging). |
| D2 Manifest and permissions | **PARTIAL — see Risks R-1** | App-own manifest declares exactly the 4 spec'd permissions (verified file content); `INTERNET` ABSENT (aapt-verified; grep: no INTERNET in `app/src`; `foregroundServiceType="mediaPlayback"` on the service; `noCompress "mp3"` working (64 STORED entries). BUT the merged manifest additionally inherits `android.permission.ACCESS_NETWORK_STATE` from `androidx.media3:media3-common`/`media3-exoplayer` 1.2.1 AARs (normal-protection; reads connectivity state only; **no network I/O possible without INTERNET**, which is absent). The spec's literal "exactly 4 … or any other network permission" wording is not met at merge level. All other manifest values PASS. |
| D3 Bundled assets and long-file spike | PASS bundling (spike: DV1) | 64 MP3s at `app/src/main/assets/audio/<id>.mp3` packaged **STORED** (aapt/unzip verified; noCompress working); covers + thumbnails bundled (64/64 dirs on disk, all JSON `cover`/`thumbnail` refs resolve); longest file class ~60 MB present. The device spike (≥50 MB MP3 plays/seeks/ends from `assets/`, screen-off 10 min, on the real Fire HD) is **DV1**; the pre-agreed `AudioSourceResolver` `COPY_TO_FILES` fallback already exists behind a strategy flag (D1), so a spike FAIL flips with no other code change. |
| D4 Install procedure and versioning | PASS docs (exercise: DV11) | `docs/delivery.md` documents: build flow, `adb install -r`, `--no-local` streamed fallback (+ free-space note ~2.5 GB), ONE-TIME `adb uninstall com.julicuentos.app` (RN signature mismatch) before first native install, versionCode monotonic-bump policy (no data migration promised). Exercising on the device → DV11. |
| D5 Device acceptance checklist (a–e) | **DEFERRED-DEVICE (S5.10; DV1,DV3–DV11)** | The five-point checklist explicitly requires the Fire HD 10:; (a) background → DV4, (b) seek → DV5, (c) cold restore → DV6, (d) parity incl. play-now-keeps-queue + timer fade → DV7–DV9, (e) build+install → DV11; supporting items DV3/DV10. Only DV2 (STORED + no-INTERNET) is host-verified and [x]. |



# 3. Static audits (evidence)

| Audit | Result | Evidence |
|---|---|---|---|
| Elevation / shadow / blur | **Absent** | `grep -rniE "elevation|shadow|blur" res/ java/` → comments/KDoc only ("no elevation", "no shadow"); no `animated-vector` files; no `*anim*` resources. |
| Hardcoded UI strings | **None in layouts**; one transient time default in Kotlin | `grep -rnE 'android:text="[^@]'` over layouts → 0 hits; Kotlin user-facing literal → 1: `SeekBarController.setDuration`: `durationLabel.text = "0:00"` — identical to `formatTime(0)`, replaced on first metadata tick; not copy. |
| Touch targets | **52 dp standard** | `min_touch=52dp` used across player, catalog, queue, timer, sheet, miniplayer (48 dp visuals inside 52 dp targets; play 72 dp, compact 64 dp); error/empty buttons min 52. |
| Desugaring-free claim | **Confirmed** | `grep "import java.time|import java.util.stream|CompletableFuture" app/src/main` → 0 hits; `java.time` usage → 0;`compileOptions` Java 17 with no `coreLibraryDesugaring` (app/build.gradle.kts). |
| Off-palette hex audit | **Clean** | Every hex literal in res XML ∈ {6 tokens + track + 4 literals (incl. alpha variants)} ∪ {#FFFFFFFF icon-glyph fills on dark surfaces} ∪ {#00000000 transparent dialog bg}. |
| R2-001 regress check | Clean | `grep Bundle.EMPTY java/` → 0 hits (Bundle() everywhere). |

# 4. Behavioral code review — critical contracts

| Contract | Verdict | What was read |
|---|---|---|---|
| Commit-on-release + pending-target hold | PASS | `SeekBarController`: drag start → dragging=true (position writes suppressed); drag moves → preview label only, ZERO player calls; release → exactly one `onCommit → repo.seekTo`; `pendingTargetMs` holds the thumb at the commit target until the player position is within ±1.5 s (R4-001) — no backward snap. |
| Load-generation guard | PASS | `loadGen` monotonic; `performLoad` captures `myGen`, `resolveAsync` callback discards when `loadGen != myGen`; controller events gated on `isCurrentItem` (stale generations never paint the UI). |
| Timer fade + endOfStory suppression | PASS | `expireTimer` arms `ACTION_TIMER_EXPIRE` only while actually playing; service `fadeRunnable` = 10 × 1 s `fadeVolumeAtStep` (0.9→0.0) then `pause()` + volume 1.0;per-step `isPlaying` check cancels into `finishFade()` (user cancel rules wired via `ACTION_TIMER_CANCEL_FADE`); `onEnded` EndOfStory branch suppresses auto-advance, clears timer,queue preserved. |
| Persistence hydration guards + user-wins + D3 anchor | PASS | `hydrate()` sets `hydrationComplete` first (re-entrancy safe); `markDirty`/`flushNow` early-return pre-hydration (never write before hydration); `userMutated` → snapshot discarded wholesale + dirty re-armed (R5-002); `endsAt` built + parsed against `SystemClock.elapsedRealtime()` (D3) with validity window + 24 h sanity cap (no self-pause). |
| Play-now preserves queue | PASS | No queue mutation anywhere in `load`/`performLoad`/sheet/catalog-tap paths; queue mutators are only `enqueue`/`moveQueueUp`/`moveQueueDown`/`removeFromQueue`/`clearQueue`/`playNext`/`onEnded`. |
| End-of-queue stop vs ⏭ wrap asymmetry | PASS | `onEnded`: takeNext(head) → autoplay;empty → `pause()` + Ended (no wrap, queue preserved);`playNext`: `takeNext(...) ?: circularNext(...)` — manual wraps the circular catalog int. Separate code paths, exactly per spec. |

# 5. Cross-artifact consistency

- **tasks.md checked items vs code:** S1.1–S5.9 all `[x]` and materialized per code reads above; S2.2/S2.3 landed as `playback/QueueStore.kt`/`playback/TimerLogic.kt` (aliases documented in tasks.md, closed [x]); S1.7 closed in slice 3 (fonts ladder+ splash by color); S1.10/S1.11/S1.12/S2.9/S3.8/S4.8/S5.10 = `[ ]` + `(deferred-device)` — genuinely device-bound; DV2 `[x]` re-verified this session (aapt + unzip).
- **apply-progress claims vs repo:** claims match — versionCode 5, 4-permission manifest + no INTERNET (modulo the new R-1 finding), fonts/ladder present, `TimeFormat` hoisted (slice-2 deviation 4 closed: StoryAdapter formats via `TimeFormat.formatTime`,), 20-story wording superseded by the owner's 64-story catalog (status + review-ledger process note), build times consistent with the recorded runs.
- **review-ledger findings actually fixed (spot-checked):**
 - R2-001 (Bundle.EMPTY→Bundle: `grep Bundle.EMPTY` → 0; `Bundle()` in notification actions/commands ✓.
 - R3-001 (inflate order: `MiniPlayerView` fields bound in `init` AFTER `inflate` ✓).
 - R4-001 (pending target: `pendingTargetMs` present + enforced in `SeekBarController` ✓).
 - R5-001 (onStop flush: `MainActivity.onStop → PlaybackRepository.flushNow()` ✓).
 - R5-002 (re-arm after user-wins discard: `markDirty()` in the `userMutated` branch of `hydrate()` ✓).
 - Also confirmed in code: R2-002 (progress slot cleared when idle), R2-003 (playerError current-truth + loadGen guard), R2-004 (resolveAsync worker thread + main-post), R2-005 (bounded reconnect on disconnect), R2-006 (catalog-duration seek clamp), R3-002 (minOf budget), R3-003 (decoded-size doc corrected), R3-004 (showFragment re-entrancy guard), R4-002/003/004, R1-002 (port-script assertions).
- **DV register:** verbatim from `docs/delivery.md` (reproduced below). DV2 host-verified; DV1, DV3–DV11 open until the device session.



# 6. Deferred-device register (verbatim from docs/delivery.md)

> Host-verified already: DV2 (STORED mp3 entries + no INTERNET permission). The remaining on-device items are open until the device session:

| id | Item | Source task |
|---|---|---|---|
| DV1 | Spike: longest asset plays/seeks/ends from `assets/`, screen-off 10 min | S1.11 |
| DV3 | Notification interactive: play/pause,−15,+15,next, artwork; survives pause at shade | S2.9 |
| DV4 | Screen-off playback ≥30 min, process alive | S5.10 |
| DV5 | Seek: no snap-back, preview visible, one seekTo on release, portrait + landscape | S4.8 |
| DV6 | Cold restore: force-stop → reopen paused at position, queue intact, unknown ids dropped | S5.10 |
| DV7 | Timer: 15/30/45 countdown survives screen-off; fade ≈10 s to pause; queue preserved; end-of-story suppresses advance | S5.10 |
| DV8 | Queue parity: append+dedup, reorder, remove, clear, manual next wraps, auto-advance stops at empty | S5.10 |
| DV9 | Grid 5/3/2 columns + no OOM fling + "Sonando" pill tracking | S3.8 |
| DV10 | Flat/fonts/touch audit: no elevation/blur, no system font, 52 dp targets, duration chips correct | S5.8 |
| DV11 | Install procedure incl. one-time RN uninstall + `--no-local` fallback documented and exercised | S5.9 |

# 7. Risks / findings found during verify

- **R-1 (PARTIAL — owner decision required): `ACCESS_NETWORK_STATE` in the merged manifest.** The pinned `androidx.media3` 1.2.1 AARs (`media3-common` + `media3-exoplayer`) declare `android.permission.ACCESS_NETWORK_STATE`. It is normal-protection; permits connectivity-state reads only (**no network I/O — INTERNET is absent, so the "cannot make a network request even by accident" guarantee HOLDS**). Options: (a) accept as library-inherent, consistent with the documented `DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION` precedent — then D2 may be signed off PASS-with-note; or (b) strip with a manifest-merge `tools:node="remove"` on that `uses-permission` and re-run DV2. Not fixed during verify (read-only mandate). Impact: spec-literal wording only; no functional or privacy impact.
- **R-2 (note): 64-story catalog scales the UI/APK.** Catalog now renders 64 cards and the APK is ~1.25 GB with 64 MP3s (~1.1 GB on disk,. All budget mechanisms (LruCache min(12 MB, heap/8), 512 px RGB_565 thumbs, 2-entry cover cache, stable-id recycling, targeted 500 ms updates) verified for this scale; landscape fling OOM check remains DV9.
.
- **R-3 (note): pre-existing warnings.** `adapterPosition` deprecation (`StoryAdapter`),and `Unreachable code` at `AudioSourceResolver.kt:110` (Kotlin non-local-return benign warning inside the copy-fallback block) — both compile-green and behaviorally inert; flagged for cleanup only.

- **R-4 (note):** `StoryRepository` parses `stories.json` lazily on the first calling thread (its doc admits the D4 worker-thread intent deferred to the restore slice; for 64 entries this is a few-ms parse, no functional impact). Also `queue_added_ok` registry string is unused,and a few dead-but-harmless dimas/styles remain (`stub_padding`, `stub_gap`, some TextAppearance unused by queue/timer screens) — noted, harmless.
- **R-5 (info):** notification artwork decode uses `BitmapDecoder.decodeSampled(…,256)` inline in `updateArtwork` on the main thread at story transitions (≤256 px power-of-two decode, ~ms on this SoC class), flagged for a future off-main move should it ever jank on-device; DV3 will confirm. |



# 8. Conclusion

All **49 host-verifiable requirements PASS** with code evidence (file:line in the tables); exactly **3 requirements are DEFERRED-DEVICE** by the recorded owner decision (DV4, DV3, and the full acceptance run D5+; device checks DV1/DV3–DV11 remain open; exactly **1 PARTIAL** (delivery D2 manifest, R-1) with **0 FAILs**. Build green (forced rerun,, 61/61 unit tests green,, MP3s STORED,, `INTERNET` absent,, static audits clean,, review-ledger fixes all present in code,, cross-artifact claims consistent.. **Verdict: PASS-WITH-DEFERRED** — ready for the Fire HD 10 acceptance session (DV1/DV3–DV11)
and the owner decision on R-1.
